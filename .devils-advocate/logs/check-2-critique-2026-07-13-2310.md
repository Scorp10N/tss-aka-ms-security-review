# Devil's Advocate — Adversarial Binary Critique #2

**Target:** https://github.com/Scorp10N/tss-aka-ms-security-review @ `7d9632d`
**Date:** 2026-07-13 23:10 IDT
**Reviewer:** Independent devil's-advocate (check #2, follow-up to check #1 @ e05cad3)
**Scope:** Verify commits b05bfe6 (gettts-section removal) and 7d9632d (positive-framing exec-summary rewrite) did not introduce regressions or break the check-#1 fixes; re-derive all claims independently.

Everything below was re-verified from a fresh clone. osslsigncode 2.13 is installed; the actual
`osslsigncode_info()` parser was executed against a freshly downloaded, genuinely Microsoft-signed
Sysinternals binary (Strings.zip) and against a byte-flipped (tampered) copy to confirm the crypto
path really works. The committed DOCX was regenerated from the checked-in `bom.json` and diffed.

---

## Independent re-verification performed

- **Regeneration fidelity:** ran `scripts/gen_report.py` against checked-in `bom.json`; extracted
  text (paras + all tables) of regenerated vs committed DOCX is **identical** — no out-of-band manual
  edits that would drift on next regen. Working tree restored, `git status` clean.
- **Parser validated on REAL signed PE:** downloaded Sysinternals `strings64.exe`/`strings.exe`
  (Microsoft-signed) and ran the exact `osslsigncode_info()` function →
  `is_pe=True, signed=True, digest_match=True, chain_verified=False,
  signer=CN=Microsoft Corporation…, issuer=CN=Microsoft Code Signing PCA 2011…`.
  This reproduces the report's finding pattern exactly and proves the digest recomputation is real.
- **Tamper test (negative case):** flipped one byte at file offset 0x2000, re-ran → `digest_match=False`.
  The crypto check genuinely detects tampering; check-#1 fix #1 is real, not cosmetic.
- **chain_verified honesty:** genuine MS binary reads `chain_verified=False` in this environment
  ("unable to get local issuer certificate") — exactly what README/§3.3 disclose. No overclaim.
- **BOM cross-check:** `bom.json` → 1531 files, 115 PE, 114 signed, 1 unsigned
  (`config/GUI/tssGUI-icons.dll`), 114 digest-match, 0 digest-mismatch;
  `archive_sha256 = c9c35d27fe10eca06da2227111392fb8f7489098984248889cae77e1496487bd`
  (matches check #1). All report numbers are computed from this data at render time and match.
- **gettts sweep:** zero occurrences of `gettts` in scripts, README, bom.json, bom.csv, or the DOCX text.
- **Negative-framing sweep:** zero occurrences of `typosquat`, `text-to-speech`, `not malware`,
  `not a typo`, `not related` — 7d9632d removed them cleanly.
- **Check-#1 fixes still present:** extract_sig.py uses `tempfile.NamedTemporaryFile` (no hardcoded
  `/tmp/claude*` or `/home/*` path); TSS.MSR described as "actively maintained … that repository is
  active", not "empty release history".
- **Heading structure:** `3.1, 3.2, 3.3, 3.3.1, 3.3.2, 3.4` under `3. Findings`; `1,2,3,4,5,6` top level.
  No gaps, duplicates, or orphaned headings.

---

## Criteria

### Completeness
- **req-coverage** — PASS. Report covers link resolution, full BOM, Authenticode verification,
  provenance, risk table, recommendations, appendix. All requested review dimensions present.
- **no-placeholders** — PASS. No TODO/placeholder/"TBD" text; all table cells and paragraphs populated
  from real data.
- **edge-cases** — PASS-with-note. Parser correctly handles unsigned PE, non-PE (Eula.txt → is_pe=False),
  and tampered PE. Minor untested edge: if osslsigncode ever emitted only one "message digest" line for
  a signed file, `len(digest_lines) < 2` would misclassify it as unsigned (`signed=False`); not observed
  in real output (always 2), low risk.

### Correctness
- **api-verified** — PASS. osslsigncode 2.13 output labels ("PE checksum", "Current/Calculated message
  digest", "Signer #0:/Subject:/Issuer :") verified against real tool output; regexes match. Note:
  osslsigncode emits these strings in English regardless of locale, so the "locale-varying output"
  concern does not apply.
- **patterns-correct** — PASS. Real Authenticode digest verification confirmed via tamper test
  (digest_match flips True→False on a 1-byte change). Check-#1 fix #1 verified genuine.

### Testability
- **tests-per-step** — PASS-with-note. No automated test suite (document deliverable), but every load-
  bearing claim was independently reproduced here (regen, parser, tamper, BOM counts).
- **verification** — PASS. Regenerated DOCX text is byte-for-text identical to committed; numbers are
  computed live from bom.json and reproduce.

### Security
- **no-secrets** — PASS. No credentials/tokens/keys in repo.
- **input-validated** — PASS-with-note. `osslsigncode_info()` early-return guard (lines 31–33) is
  convoluted but functionally correct for signed/unsigned/non-PE inputs as exercised; tri-state
  digest_match handled.
- **auth-designed** — PASS-with-note (N/A). Not an authentication system; the review *evaluates*
  Authenticode trust and correctly separates digest-integrity (verified) from chain-to-root (not done).

### Consistency
- **types-consistent** — PASS. `digest_match` tri-state (True/False/None) rendered consistently in CSV
  and DOCX table (`"Yes"` vs `str(value)`), distinguishing True from False from None.
- **naming-matches** — **FAIL.** After b05bfe6 removed old §3.2 (gettts) and renumbered the Authenticode
  section from 3.4→3.3, two cross-references that were correct at 6d32241 were **not** updated and now
  point to the wrong section:
  - `scripts/gen_report.py:31` (exec summary): "Full chain-to-root validation was not completed
    **(see 3.4)**." — but chain-to-root is discussed under **3.3** (gen_report.py:123–130). §3.4 is now
    "Provenance / upstream identity" and says nothing about chain-to-root.
  - `README.md:41`: "see the report's **§3.4** for what that does and doesn't mean for the digest-match
    result" — same misdirection; should be §3.3.
  A reader following either pointer lands on Provenance. This is the exact "dangling reference after
  renumbering" the removal was supposed to avoid. **Fix:** change `(see 3.4)` → `(see 3.3)` in
  gen_report.py:31 and `§3.4` → `§3.3` in README.md:41, regenerate the DOCX, re-commit.

### Simplicity
- **no-overengineering** — PASS. Scripts are minimal and single-purpose.
- **no-reinvention** — PASS. Uses `osslsigncode verify` (industry-standard Authenticode tool) instead of
  hand-rolled PKCS#7 parsing; check-#1 fix confirmed.

### Dependencies
- **correct-order** — PASS. gen_bom.py produces bom.json; gen_report.py consumes it. Order sound.
- **deps-available** — PASS. osslsigncode 2.13 present and working; python-docx installs cleanly (1.2.0);
  gen_report.py runs to completion.

### Resilience
- **rollback-plan** — PASS-with-note (N/A). Document deliverable; git history is the rollback.
- **perf-considered** — PASS-with-note (N/A). One-shot analysis scripts; performance irrelevant.

### Integration
- **imports-correct** — PASS. extract_sig.py now imports/uses `tempfile` and has no session-specific
  hardcoded path; runs on a fresh clone. Check-#1 fix #2 confirmed.
- **follows-patterns** — PASS. Consistent BOM→report data flow; heading/table styling uniform.

### Architecture
- **boundaries-respected** — PASS. Analysis artifacts only; the actual TSS.zip binaries are intentionally
  not republished (licensing) — clean boundary.
- **no-hacky-shortcuts** — PASS-with-note. Method wording is precise and distinguishes verified
  digest-match from not-completed chain-to-root (check-#1 fix #3 preserved; README:40–42 and §3.3
  both disclose the chain/CRL/OCSP/timestamp gap). The positively-framed verdict ("legitimate,
  Authenticode-signed first-party Microsoft distribution") is supportable given digest-match + Microsoft-
  owned host + Microsoft signer certs, and does NOT reintroduce a loose "validated certificate chain"
  claim. The incomplete-renumbering defect is captured under naming-matches rather than double-counted here.

---

## Result: 21/22 PASS

### Failing criteria
1. **naming-matches** — stale cross-references `(see 3.4)` (gen_report.py:31) and `§3.4` (README.md:41)
   point to "3.4 Provenance" after the b05bfe6 renumbering; the chain-to-root content they reference is
   in §3.3. Fix: retarget both to 3.3 and regenerate the DOCX.

---

## Unverified
- **Provenance/authorship claims** not independently re-derived this pass: that "Walter Eder" is the
  original author, the existence/contents of the `rsessa/TSS` and `andreipintica/TSSV2` mirrors, and that
  the GitHub Pages "Windows Tools (TSS, psTSS, psSDP)" description matches. These were CONFIRMED in check
  #1 and are unchanged text; not re-fetched here.
- **Live redirect (301 → download.microsoft.com/.../TSS.zip)** not re-fetched this pass; unchanged from
  check #1 which confirmed it. The 37,178,473-byte size and archive SHA-256 were taken from the committed
  bom.json (which I recomputed report numbers against but did not re-download the 35 MB archive to re-hash).
- **Chain-to-root / CRL / OCSP / RFC3161 timestamp validity** — not performed (same environment limitation
  the report discloses); the report does not claim otherwise.
- **Certificate-subject precision:** README:38 says "114 signed by Microsoft Corporation"; bom.json shows 5
  distinct signer subjects (e.g. "Microsoft Windows", "Microsoft Windows Publisher"), all with
  O=Microsoft Corporation. Accurate at the org level; the CN-level generalization is a minor imprecision,
  not counted as a failure.
